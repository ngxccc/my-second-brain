Đừng chỉ dùng `any`. Học về **Generics**, **Utility Types** (`Pick`, `Omit`, `Partial`).
Hiểu về **Structural Typing** của TS (vịt nào kêu quạc thì là vịt) khác gì với **Nominal Typing** của Java/C#.
**MVC** hoặc **Layered Architecture** (Controller -> Service -> Repository)

#### Option 1: Backend Heavy (Main: Node.js / Sub: React)

_Hợp với ông nếu: Thích logic, thuật toán, dữ liệu, ghét chỉnh CSS lệch 1px._

- **Độ sâu (Deep Dive - Cần học cực kỹ):**
    
    - **Architecture:** NestJS, Clean Architecture, Design Patterns (Singleton, Factory, Observer).
        
    - **Performance:** Node.js Event Loop, Memory Leaks, Streams (xử lý file lớn), Worker Threads.
        
    - **Database:** Indexing, ACID, Transaction, N+1 Query problem.
        
    - **System:** Caching (Redis), Message Queue (RabbitMQ), Docker sâu.
        
- **Độ rộng (Just enough - Chỉ cần biết dùng):**
    
    - Frontend: Biết dùng Next.js để fetch data (SWR/TanStack Query), biết chia component cơ bản, dùng UI Library (Antd/Mantine) cho lẹ chứ không ngồi viết CSS từ đầu.

design partern là gì

từ dự án hiện tại thì cần áp dụng design partern, system design, architecture gì là phù hợp?